{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run test:co-creation && pnpm run build",
  "crons": [
    {
      "path": "/api/cron/crawl",
      "schedule": "0 18 * * *"
    }
  ]
}
